import { Layout } from "../components/layout";

const Login = () => {
  return (
    <Layout>
      <div className="bg-gray-400 rounded-sm font-extrabold">
        <button>Click here to login</button>
      </div>
    </Layout>
  );
};

export default Login;
